<%= render 'layouts/section', user: current_user, book: @book_new %>

<h2>Books</h2>
<table>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <%= render 'book_table', book: book %>
    </tr>
    <% end %>
  </tbody>
</table>