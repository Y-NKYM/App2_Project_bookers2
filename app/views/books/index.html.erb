<%= render 'new', book: @book %>

<h2>Books</h2>
<table>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <%= render 'book_table', book: book %>
    </tr>
    <% end %>
  </tbody>
</table>